{
  "author": "Lars Willighagen",
  "private": true,
  "version": "0.1.1",
  "scripts": {
    "deploy": "./deploy",
    "deploy:dev": "npm run deploy -- dev",
    "deploy:stage": "npm run deploy -- stage",
    "deploy:prod": "npm run deploy -- prod",
    "deploy-all": "for f in .keys/*; do k=\"${f##*/}\" && npm run deploy -- $k; done",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "html-entities": "^1.2.1"
  }
}
